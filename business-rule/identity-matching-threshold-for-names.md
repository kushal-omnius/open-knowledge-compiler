---
type: business_rule
title: "Identity matching thresholds"
slug: business-rule/identity-matching-threshold-for-names
repo: knowledge-compiler
compile_run: 3741
commit: b632594b2c40a3aa8020c6ef08354192e9a481b7
files:
  - knowledge_compiler/compiler/normalize.py
generated:
  by: process:knowledge-compiler/1.3.0
  at: 2026-09-01T13:40:03.516353+00:00
---

<!-- Generated by Knowledge Compiler — do not edit. The wiki is a build artifact (ADR-010); fix the source or the compiler. -->

# Identity matching thresholds

The threshold for anchor similarity is set to 0.5 and for name similarity is set to 0.8.

To ensure conservative and consistent identity matching, prioritizing over-splitting rather than over-merging to maintain uniqueness of identities.

**Anchored to:**

- `knowledge_compiler/compiler/normalize.py` — `knowledge_compiler.compiler.normalize.Thresholds`

## Governs

- [knowledge_compiler.compiler.normalize](../component/knowledge-compiler-compiler-normalize.md)

## Recent history

- 2026-09-01 (**Update**, compile run 3741, `b632594b2c40`) — `intent`: 'To ensure conservative matches that avoid merging disparate identities.' → 'To ensure conservative and consistent identity matching, prioritizing over-splitting rather than over-merging to maintain uniqueness of identities.', `name`: 'Identity-matching thresholds' → 'Identity matching thresholds', `related_components`: [] → ['knowledge_compiler.compiler.normalize'], `statement`: 'When matching identities, an anchor overlap score must be at least 0.5 and a name similarity score must be at least 0.8.' → 'The threshold for anchor similarity is set to 0.5 and for name similarity is set to 0.8.'
- 2026-08-26 (**Update**, compile run 2912, `575a64afc0d8`) — `intent`: 'To ensure that identity assignments are conservatively determined, avoiding erroneous merges.' → 'To ensure conservative matches that avoid merging disparate identities.', `name`: 'Identity-matching configuration' → 'Identity-matching thresholds', `statement`: 'Identity matching thresholds are set so that anchor overlap must be at least 0.5 and name similarity must be at least 0.8.' → 'When matching identities, an anchor overlap score must be at least 0.5 and a name similarity score must be at least 0.8.'
- 2026-08-24 (**Update**, compile run 2448, `b632594b2c40`) — `intent`: 'To prevent incorrect identity assignments during normalization.' → 'To ensure that identity assignments are conservatively determined, avoiding erroneous merges.', `name`: 'Identity Matching Threshold for Anchors' → 'Identity-matching configuration', `statement`: 'Anchor overlap score must be at least 0.5 for identity matching.' → 'Identity matching thresholds are set so that anchor overlap must be at least 0.5 and name similarity must be at least 0.8.'
