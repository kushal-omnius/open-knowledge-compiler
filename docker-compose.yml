services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: kc
      POSTGRES_PASSWORD: kc
      POSTGRES_DB: kc_wiki
    ports:
      - "5432:5432"
    volumes:
      - kc_pgdata:/var/lib/postgresql/data

volumes:
  kc_pgdata:
